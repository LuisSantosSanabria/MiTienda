@model UsuarioVM;
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <title>@ViewData["Title"] - MiTienda</title>
</head>
<body class="bg-body-secondary">
    <div class="container">
        <div class="row justify-content-center align-items-center vh-100">
            <div class="col-md-6 col-lg-4">
                <div class="card show">
                    <div class="card-body p-4">
                        <h2 class="card-title text-center mb-4">Registro</h2>

                        <form action="/Registro/Registro" method="post">

                            <div class="mb-3">
                                <label class="form-label">Nombre</label>
                                <input class="form-control" type="text" asp-for="Nombre" />
                                <span asp-validation-for="Nombre" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Email</label>
                                <input class="form-control" type="email" asp-for="Email" />
                                <span asp-validation-for="Email" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Clave</label>
                                <input class="form-control" type="password" asp-for="Clave" />
                                <span asp-validation-for="Clave" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Repetir Clave</label>
                                <input class="form-control" type="password" asp-for="RepetirClave" />
                                <span asp-validation-for="RepetirClave" class="text-danger"></span>
                            </div>
                            <input type="hidden" asp-for="Tipo" value="Clientes" />

                            @if (ViewBag.message != null)
                            {
                                <div class="alert @ViewBag.Class" role="alert">
                                    @ViewBag.message
                                </div>
                            }

                            <div class="d-grid gap-2">
                                <button class="btn btn-primary" type="submit">Registrar</button>
                                <a href="/Registro/Login" class="btn btn-outline-secondary">Volver</a>
                            </div>

                        </form>

                    </div>
                </div>

            </div>
        </div>
    </div>
</body>
</html>